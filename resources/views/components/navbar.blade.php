<div class="w-full h-20 border-b border-black flex">
    <div class="w-full flex justify-between items-center max-w-3xl mx-auto">
        <a href="{{ route('welcome') }}" class="text-2xl font-bold">Laravel 8 CRUD</a>
        <div class="flex gap-4 font-semibold text-xl">
            <a href="{{ route('login') }}">Login</a>
            <a href="{{ route('register') }}">Register</a>
        </div>
    </div>
</div>
